# 测试记录

本地规则评估：`uv run xiaod eval`

已覆盖：

- 小宇宙 / B 站 / YouTube 分类进小 D
- 小红书 / 抖音 / 公众号拒绝并说明只做音视频
- 错词表与时间戳清洗
- 提纯稿 QA（拒摘要、拒垃圾桶标题）
- 文档标题格式
- FastAPI 任务提交、空输入、忙时拒绝
- 用户面文案不含堆栈和状态码

本地已跑通（2026-09-14）：

- `pytest tests`：26 passed
- `uv run xiaod eval`：4 条样本分类全 PASS
- `npm run build`：Vue 控制台打包成功

端到端（需要密钥和 ffmpeg）：

1. 填 `LANGSMITH_API_KEY` 后再跑 `uv run xiaod hello`，在项目 `xiaod` 看到 hello trace
2. 样例 1：短播客 `uv run xiaod run "请转录 <小宇宙链接>"`
3. 样例 2：B 站公开视频
4. 样例 3：较长 YouTube（可标 skip-slow）
5. 网页：`uv run xiaod web` + `cd web && npm run dev`，同一条链接走控制台
6. 飞书私聊发同一条链接，确认文档可打开且自己有权限
